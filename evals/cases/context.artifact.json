{
  "manifest_version": 1,
  "case_id": "context.artifact",
  "title": "Artifact-backed context keeps stable invariants",
  "bundle_path": "../bundles/context.artifact.json",
  "tags": ["context", "artifact"],
  "notes": "Tracks pytest failure-digest context while ignoring run_tests output chunk differences that replay does not reproduce.",
  "release_contract": {
    "owner": "runtime.context",
    "capabilities": ["artifact_backed_context", "context_drift_detection"],
    "severity": "medium",
    "verification_stages": ["advisory"],
    "baseline_refresh_policy": "advisory"
  },
  "expectation": {
    "mode": "selected_invariants",
    "invariants": ["transcript", "tool_calls", "approvals", "final_state"]
  }
}
